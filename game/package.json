{
  "name": "h5-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@h5unity/engine": "file:../engine"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
